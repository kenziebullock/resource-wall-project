<% include ./head.ejs %>
<nav class="navbar navbar-expand-md bg-dar">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse"

    aria-expanded="false" aria-label="Toggle navigation"></button>
    <span class="navbar-toggler-icon"></span>
  </button>


      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <form action="/logout" method="post">
              <button type="submit" class="btn">Logout </button>
            </form>
          </li>
          <li class="nav-item">
            <form action="/users/<%=  %>" method="get">
              <button type="submit" class="btn">Profile</button>
            </form>
          </li>
          <li class="nav-item">
            <form action="/resource_show" method="get">
              <button type="submit" class="btn">My Posts</button>
            </form>
          </li>
          <li class="nav-item">
            <form action="/users/" method="get">
              <button type="submit" class="btn">My Likes</button>
            </form>
          </li>
          <li class="nav-item">
            <form action="/create_resource/" method="get">
              <button type="submit" class="btn">New Post</button>
            </form>
          </li>
          <li>
            <span class="navbar-brand mb-0 h1">Welcome, </span>
          </li>
        </ul>
        <form class="form-inline mt-2 mt-md-0" for="query" action="/search" method="get">
          <input class="form-control mr-sm-2" name="query" type="search" placeholder="Search">
          <button class="btn my-2 my-sm-0" type="submit">Search</button>
        </form>
      </div>
</nav>

<% if (success && success.length > 0) { %>
  <br>
  <div class="container">
      <div class="alert alert-success" role="alert"><%= success %></div>
  </div>
<% } %>
<% if (error && error.length > 0) { %>
  <br>
  <div class="container">
      <div class="alert alert-danger" role="alert"><%= error %></div>
  </div>
<% } %>
